/* */ 

const names = ["Hole in one!", "Eagle", "Birdie", "Par", "Bogey", 
"Double Bogey", "Quit"];

let par = 0;
let strokes = 0;

switch (strokes) {
    case "Hole in one!":
        strokes == par; 
        console.log(names[0]);
        break;
}